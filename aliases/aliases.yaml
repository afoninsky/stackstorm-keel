name: trigger
description: Manually trigger event of updating image
action_ref: keel.trigger.image
ack:
  enabled: false
formats:
  - keel trigger {{ image }}:{{ tag }}
---
name: approve
description:
action_ref: keel.approve
ack:
  enabled: false
formats:
  - keel approve {{ image }}:{{ tag }}
parameters:
  action: approve
  voter: hubot
---
name: reject
description:
action_ref: keel.approve
ack:
  enabled: false
formats:
  - keel reject {{ image }}:{{ tag }}
parameters:
  action: reject
  voter: hubot
---
name: delete
description:
action_ref: keel.approve
ack:
  enabled: false
formats:
  - keel delete {{ image }}:{{ tag }}
parameters:
  action: delete
  voter: hubot
